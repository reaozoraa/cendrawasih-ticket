import React from "react";
import Layout from "./layout";

import {
  Box,
  Typography,
  Container,
  Button,
  Checkbox,
  FormControl,
  FormControlLabel,
  FormLabel,
} from "@mui/material/";

function bookmarks() {
  return (
    <Layout>
      <Container
        maxWidth="auto"
        sx={{
          position: "relative",
          backgroundColor: "white",
          height: "100vh",
          // position: "absolute",
          color: "black",
        }}
      >
        test
      </Container>
    </Layout>
  );
}

export default bookmarks;
